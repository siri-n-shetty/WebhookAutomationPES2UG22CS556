# Webhook Automation - Bajaj Finserv Health | Qualifier 1 | JAVA

## Overview
A Spring Boot app that:
- Generates a webhook and JWT on startup.
- Solves a SQL problem.
- Automatically submits the final SQL query to the webhook.

## Run Instructions
```bash
mvn clean package 
java -jar target/WebhookAutomationPES2UG22CS556-0.0.1-SNAPSHOT.jar
